apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: metallb
  namespace: argocd

spec:
  project: default

  destination:
    server: https://kubernetes.default.svc
    namespace: metallb-system

  sources:
    - repoURL: https://metallb.github.io/metallb
      chart: metallb
      targetRevision: "0.15.2"

      helm:
        releaseName: metallb
        valueFiles:
          - $values/kubernetes/apps/metallb/values.yaml

    - repoURL: https://github.com/luren9/home-server.git
      targetRevision: main
      ref: values

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
